name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: printenv
        run: printenv  
  docs:
    runs-on: ubuntu-latest
    container:
      image: pseudomuto/protoc-gen-doc
#       volumes:
#         - ${{GITHUB_WORKSPACE}}/docs/:/out
#         - ${{GITHUB_WORKSPACE}}beer.proto:/protos        
    steps:  
      - name: protoc-gen-doc 
        run: printenv
